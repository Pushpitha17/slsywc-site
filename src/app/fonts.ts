import { Inter, Orbitron, VT323, Silkscreen} from "next/font/google"

export const inter = Inter({
  subsets: ["latin"],
  display: "swap"
})

export const vt323 = VT323({
  subsets: ["latin"],
  display: "swap",
  weight: ["400"]
})

export const orbitron = Orbitron({
  subsets: ["latin"],
  display: "swap"
})

export const silkscreen = Silkscreen({
  subsets: ["latin"],
  display: "swap",
  weight: ["400"]
})